{
  "name": "arus",
  "version": "6.0.0",
  "private": true,
  "description": "ARUS v6 — asisten riset IDX multi-agent: ticker universal, dua router (heuristik+LLM multi-intent), verifier grounding, hemat kredit.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch server.ts",
    "start": "tsx server.ts",
    "build": "tsc -p tsconfig.json",
    "eval": "tsx eval/run.ts",
    "calib": "tsx calib/run.ts",
    "mcp": "tsx mcp/server.ts",
    "test": "npm run eval"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^22.7.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  }
}
